# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-single
artifact
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local secrets
.env
.env.local

# Android release signing key — losing this means the app can never be
# updated on Play Store again under the same package; back it up somewhere
# safe outside git, never commit it.
android-signing/
*.keystore
*.jks

# Generated TWA/Capacitor Android project (built on your own machine, not here)
android/
